from django.urls import path, re_path

# from presensi.views import JabatanView, OrderView, PegawaiView, ShiftView

# urlpatterns = [
#     re_path(r'^orders/(?:(?P<pk>\d+)/)?(?:(?P<action>\w+)/)?', OrderView.as_view(), name='orders'),
#     re_path(r'^office/(?:(?P<pk>\d+)/)?(?:(?P<action>\w+)/)?', JabatanView.as_view(), name='office'),
#     re_path(r'^shifts/(?:(?P<pk>\d+)/)?(?:(?P<action>\w+)/)?', ShiftView.as_view(), name='shifts'),
#     re_path(r'^emplo/(?:(?P<pk>\d+)/)?(?:(?P<action>\w+)/)?', PegawaiView.as_view(), name='employee'),
# ]
